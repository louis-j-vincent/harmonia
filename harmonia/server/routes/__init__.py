"""Un fichier par groupe de routes API — voir chaque module pour son domaine.

Ce que ce paquet ne fait PAS : construire l'app ni servir les routes
statiques (`/`, `/audio`, `/pwa`, `/reports`, `/plots`, `/audiotest`) — c'est
`harmonia.server.app`.
"""
